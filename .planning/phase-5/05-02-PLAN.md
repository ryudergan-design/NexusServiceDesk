---
phase: 05-ia-rag
plan: 02
type: execute
wave: 2
depends_on: ["05-ia-rag-01"]
files_modified: ["src/lib/ai/agents/triage.ts", "src/lib/ai/agents/collection.ts", "src/lib/ai/rag/engine.ts"]
autonomous: true
requirements: ["AI-TRIAGE", "AI-COLLECT", "AI-RAG"]
required_skills: ["ai-architect", "database-performance"]

must_haves:
  truths:
    - "O Agente de Triagem classifica automaticamente novos chamados."
    - "O Agente de Coleta identifica lacunas de informação e sugere perguntas."
    - "A engine de RAG recupera artigos relevantes usando FTS5."
  artifacts:
    - path: "src/lib/ai/agents/triage.ts"
      provides: "Server Action para triagem automática"
    - path: "src/lib/ai/agents/collection.ts"
      provides: "Lógica de identificação de lacunas de dados"
    - path: "src/lib/ai/rag/engine.ts"
      provides: "Busca híbrida (SQL + Contexto IA) de artigos"
  key_links:
    - from: "src/lib/ai/agents/triage.ts"
      to: "src/lib/ai/config.ts"
      via: "Groq Client"
    - from: "src/lib/ai/rag/engine.ts"
      to: "prisma.$queryRaw"
      via: "FTS5 MATCH query"
---

<objective>
Implementar a inteligência de entrada do sistema: triagem automática de chamados, identificação de lacunas de informação (coleta) e a engine de busca contextual (RAG).

Objetivo: Automatizar o processamento inicial do chamado e fornecer contexto para a resolução.
Saída: Agentes de Triagem, Coleta e Engine de RAG funcionais.
</objective>

<execution_context>
@C:/Users/AMC/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-5/05-01-PLAN.md
@.planning/phase-5/RESEARCH.md
@.planning/phase-5/CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agente de Triagem e Classificação</name>
  <files>src/lib/ai/agents/triage.ts</files>
  <behavior>
    - Entrada: { title: string, description: string }.
    - Comportamento: Usa Groq/Llama 3.3 para classificar categoria, prioridade e gerar sumário.
    - Saída: Deve respeitar o TriageSchema (Zod).
  </behavior>
  <action>
    - Implementar a função `runTriageAgent`.
    - Integrar com o AI SDK (generateObject).
    - Adicionar log de execução na tabela `AILog`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/triage.test.ts</automated>
  </verify>
  <done>Agente de triagem classificando chamados com precisão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agente de Coleta (Detecção de Lacunas)</name>
  <files>src/lib/ai/agents/collection.ts</files>
  <behavior>
    - Entrada: { title, description, attachments }.
    - Comportamento: Verifica se informações cruciais (modelo, erro, passos) estão presentes.
    - Saída: { isComplete, missingInfo[], questions[] }.
  </behavior>
  <action>
    - Implementar a função `runCollectionAgent`.
    - Criar prompts específicos por categoria de chamado (ex: TI vs. RH).
    - Retornar perguntas dinâmicas para o usuário.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ai/collection.test.ts</automated>
  </verify>
  <done>Agente de coleta identificando dados faltantes de forma inteligente.</done>
</task>

<task type="auto">
  <name>Task 3: Motor de RAG (Busca de Conhecimento)</name>
  <files>src/lib/ai/rag/engine.ts</files>
  <action>
    - Criar função `retrieveKnowledge` que usa `prisma.$queryRaw` para buscar na tabela FTS5.
    - Implementar ranking de relevância simples via SQLite rank.
    - Criar wrapper para o Agente de RAG (Gemini) processar os resultados e retornar o contexto mais útil para o chamado atual.
  </action>
  <verify>
    <automated>test -f src/lib/ai/rag/engine.ts && grep "MATCH" src/lib/ai/rag/engine.ts</automated>
  </verify>
  <done>Engine de RAG integrada ao SQLite e Gemini.</done>
</task>

</tasks>

<verification>
Executar testes unitários de IA criados durante as tarefas.
</verification>

<success_criteria>
- Novos chamados podem ser classificados automaticamente sem intervenção humana.
- O sistema detecta se um chamado está "incompleto" e gera perguntas de esclarecimento.
- A busca por artigos da base de conhecimento retorna resultados via FTS5.
</success_criteria>

<output>
Após a conclusão, crie .planning/phases/05-ia-rag/05-02-SUMMARY.md
</output>
