---
phase: 05-ia-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["prisma/schema.prisma", "src/lib/ai/config.ts", "src/lib/ai/schemas.ts", "src/app/api/ai/orchestrator/route.ts"]
autonomous: true
requirements: ["AI-CORE", "AI-SCHEMA"]
required_skills: ["ai-architect", "database-performance"]

must_haves:
  truths:
    - "O banco de dados possui suporte para busca FTS5 e tabela de base de conhecimento."
    - "Os providers Groq e Gemini estão configurados e acessíveis via Server Actions."
    - "Existem contratos Zod definidos para a entrada e saída de cada agente."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modelos KnowledgeArticle e configurações de IA no User"
    - path: "src/lib/ai/config.ts"
      provides: "Configuração do Vercel AI SDK com Groq e Gemini"
    - path: "src/lib/ai/schemas.ts"
      provides: "Zod Schemas para Triagem, Coleta, Solucionador e Curadoria"
  key_links:
    - from: "src/lib/ai/config.ts"
      to: "process.env.GROQ_API_KEY"
      via: "env var"
    - from: "prisma/schema.prisma"
      to: "SQLite FTS5"
      via: "Manual SQL migration triggers"
---

<objective>
Estabelecer a base técnica para a inteligência agêntica, incluindo a infraestrutura de banco de dados (FTS5), configuração de SDKs de IA e definição de contratos de dados (Zod).

Objetivo: Garantir que os agentes tenham um ambiente estável e contratos claros para operar.
Saída: Schema atualizado, SDK configurado e Schemas de validação prontos.
</objective>

<execution_context>
@C:/Users/AMC/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-5/RESEARCH.md
@.planning/phase-5/CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Database, Schema e SQLite FTS5</name>
  <files>prisma/schema.prisma, prisma/migrations/xxx_add_ai_and_knowledge.sql</files>
  <action>
    - Adicionar ao `User`: `aiEnabled` (boolean, default: true).
    - Criar modelo `KnowledgeArticle`: `id`, `title`, `content`, `categoryId`, `tags`, `createdAt`, `updatedAt`.
    - Criar modelo `AILog`: `id`, `agentName`, `ticketId`, `input`, `output`, `tokens`, `latency`, `createdAt`.
    - Gerar migração Prisma.
    - Criar arquivo SQL manual para habilitar FTS5 no SQLite para a tabela `KnowledgeArticle` (conforme RESEARCH.md) e adicionar triggers de sincronização.
  </action>
  <verify>
    <automated>npx prisma validate && ls prisma/migrations/*/migration.sql</automated>
  </verify>
  <done>Schema atualizado e migração de FTS5 preparada.</done>
</task>

<task type="auto">
  <name>Task 1b: Configuração de AI SDK e Providers</name>
  <files>src/lib/ai/config.ts, .env.example</files>
  <action>
    - Instalar dependências: `ai`, `@ai-sdk/openai` (para Groq via compatibility), `@ai-sdk/google`.
    - Configurar cliente Groq (usando Llama 3.3 70B para tarefas rápidas).
    - Configurar cliente Gemini (usando Gemini 1.5/2.5 Flash para RAG e Curadoria).
    - Criar wrapper de orquestração básico que permite alternar entre providers com fallback.
  </action>
  <verify>
    <automated>test -f src/lib/ai/config.ts && grep "google" src/lib/ai/config.ts</automated>
  </verify>
  <done>SDK de IA configurado com suporte a múltiplos providers.</done>
</task>

<task type="auto">
  <name>Task 2: Contratos de Dados dos Agentes (Zod)</name>
  <files>src/lib/ai/schemas.ts</files>
  <action>
    - Definir `TriageSchema`: { categoryId, priority, tags, urgency, summary }.
    - Definir `CollectionSchema`: { missingInfo: string[], questions: string[], isComplete: boolean }.
    - Definir `SolverSchema`: { solution: string, relevantArticleIds: string[], confidence: number }.
    - Definir `CurationSchema`: { title: string, content: string, categoryId: string, tags: string[] }.
    - Definir `SentimentSchema`: { score: number, sentiment: 'POSITIVE' | 'NEUTRAL' | 'NEGATIVE', insights: string[] }.
  </action>
  <verify>
    <automated>npx tsc src/lib/ai/schemas.ts --noEmit</automated>
  </verify>
  <done>Contratos Zod para todos os agentes definidos e tipados.</done>
</task>

</tasks>

<verification>
npx prisma generate
npm test (se houver testes de infra)
</verification>

<success_criteria>
- Banco de dados suporta KnowledgeArticle e logs de IA.
- FTS5 ativo (verificável via query raw).
- Server actions podem importar os clientes de IA sem erros.
- Schemas Zod exportados e prontos para uso em Structured Outputs.
</success_criteria>

<output>
Após a conclusão, crie .planning/phases/05-ia-rag/05-01-SUMMARY.md
</output>
